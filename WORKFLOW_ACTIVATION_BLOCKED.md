# Workflow Activation Pending

The canonical workflow definition is preserved at `docs/pending-workflows/x1-bootstrap-ci.yml`. GitHub rejected activation because the connected OAuth token lacks the separate `workflow` scope. Activate only after the new least-privilege Strategic-Minds-AI GitHub App is installed with Workflows read/write permission and an independent validation receipt passes.
