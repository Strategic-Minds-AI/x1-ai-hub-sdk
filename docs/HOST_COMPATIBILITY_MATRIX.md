# Host Compatibility Matrix

Maintain one row per supported host: ChatGPT, Claude, Gemini, Cursor, Windsurf/custom MCP. Record transport/auth method, supported tools/resources, streaming behavior, UI limitations, tested protocol version, last verified date and conformance receipt. Do not claim compatibility until the host-specific eval passes.
